# SHIRO Diff v007 変更仕様（動的ルール＋AI示唆）

対象ファイル: `shiro-diff.html`（v006→v007）

v006 の差分比較・SEO/E-E-A-T・構造化データ・文字カウントは**一切変更なし**。
§6.1 表記・ブランドルール／§6.2 薬機法 を「動的化＋AI示唆」に拡張した。

## 1. 3層フォールバック構成

```
[L1] AI示唆（任意・APIキー設定時）  … 文脈理解・表記揺れ・自然な書き換え
[L2] 動的ルール（起動時にシートCSV取得） … 正本スプレッドシートを実行時反映
[L3] 内蔵ルール（常時・完全ローカル）   … v006相当のフォールバック
```

- **APIキー未設定なら完全に v006 と同じ＝外部送信ゼロ**（機密原稿はキー空で運用）。
- シート取得は「ルールを読むだけ」で原稿は送信しない。

## 2. ルールの動的化（L2）

- 起動時に「SHIRO独自」「薬機法」シートの公開CSVを `fetch` して取得（ユーザー選択：起動時自動取得）。
- 取得成功でキャッシュ（localStorage）。次回は即時キャッシュ反映→ネット再取得。失敗時は内蔵ルール。
- 設定欄に各シートの公開CSV URL（2本）・取得状況ピル・「シートから最新取得」ボタンを追加。
- **薬機法シート**: `NG表現 / NG区分 / OK表現` を厳密一致ルールへ。
- **SHIRO独自シート**: `検出例 / 区分 / 修正提案` をリテラルルール化し、内蔵の高度な正規表現ルールへ上乗せ合成。
- ヘッダー名は表記揺れに耐える自動判定。判別不能なCSVは無視して既存ルールを壊さない。
- 既存の安全機構（URL内マッチ除外・範囲ベース重複除去）は維持。

### 公開CSV URL の作り方（タブごと）

1. スプレッドシートで ファイル → 共有 → ウェブに公開
2. 「リンク」タブで**個別シート**（例「SHIRO独自」）を選択、形式「カンマ区切り（.csv）」、公開
3. URL（`.../pub?...output=csv`）に各タブの `gid` を付けて `...&single=true&gid=<gid>&output=csv` とする
   - `gid` は通常の編集URLで各タブをクリックした時の `#gid=...` で確認できる
4. 「SHIRO独自」「薬機法」それぞれの URL を設定欄へ貼る

> 注意: `gid` 無しの `pub?output=csv` は**先頭シート1枚のみ**を返す。2タブを別々に読むには gid 付き URL が各1本必要。

## 3. AI示唆（L1・Claude API オプトイン）

- 設定欄に APIキー（任意保存・既定オフ）／モデル選択／実行モードを追加。
- **モデル既定 `claude-sonnet-4-6`（精度重視）**、`claude-opus-4-8`（最高精度）/`claude-haiku-4-5`（高速）を選択可。
- 実行：**比較・分析と同時に自動**（ユーザー選択）。キー未設定／無効時はスキップ。
- フロー：機械判定を即表示 → APIへ「シート由来ルール＋行番号付き原稿」を送信 → 構造化（tool使用）で
  `line / text / severity / rule / suggestion / reason` を受領 → 校正タブ下部の「AI示唆」ブロックに表示。
- 表記揺れ（例「シミが消える／シミがなくなる」）の汎化検出が主目的。ルートにない創作はさせない。
- ブラウザ直叩き：`anthropic-dangerous-direct-browser-access: true`、CORS対応。
- 失敗（キー無効・CORS・オフライン）時は機械判定のみで正常終了。

## 4. プライバシー表示

- 設定欄に常時注記：「既定は完全ローカル。AI示唆を有効化した場合のみチェック対象テキストが Anthropic API へ送信」。

## 5. 制約

- AI示唆は付加価値であり、機械判定を置き換えない。最終判断は人間レビュー前提。
- SHIRO独自の文脈依存ルール（媒体例外等）は AI 層が担当。機械層は内蔵＋シートのリテラル一致まで。
- 公開CSV URL とAPIキーが正しく設定されていることが L2/L1 の前提。
